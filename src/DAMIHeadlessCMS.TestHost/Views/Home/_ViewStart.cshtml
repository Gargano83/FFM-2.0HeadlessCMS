@{
    // Scoperto solo a questa cartella (Views/Home): NON tocca il _ViewStart.cshtml
    // radice, quindi non ha alcun effetto sulle view del backoffice (/dami),
    // servite dalla Razor Class Library DAMIHeadlessCMS.Admin.
    Layout = "_PublicLayout";
}
